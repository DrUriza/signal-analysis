from __future__ import annotations

import pandas as pd


def kalman_filter_placeholder(series) -> pd.Series:
    """
    Placeholder for future Kalman filtering utilities.
    """
    return pd.Series(series, dtype=float, name="kalman_placeholder")
